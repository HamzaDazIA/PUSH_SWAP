#include "push_swap.h"
# include <unistd.h>    



int main(int argc, char **argv)
{
    if(argc == 1)
        return(write(2,"Error",5));
    return 0;
    int i = 1;

    while(i <= argc)
    {
        check
        i++;
    }
}