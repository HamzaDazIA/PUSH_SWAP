#include "push_swap.h"

int check_malloc(t_stack **stack_a, t_stack *stack_b)
{
    if (stack_a == NULL || stack_b == NULL)
        return (-1);
    else if (stack_a == NULL && stack_b == NULL)
        return(-1);
    else 
        return (-1);                         
}

int check_return_split()